import styled from "styled-components";

const AdWrapper = styled.section`
  height: 80px;
`;

const Advert = () => {
  return (
    <AdWrapper>
      <span />
    </AdWrapper>
  );
};

export default Advert;
